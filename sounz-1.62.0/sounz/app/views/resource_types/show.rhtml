<% for column in ResourceType.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @resource_type.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @resource_type %> |
<%= link_to 'Back', :action => 'list' %>
